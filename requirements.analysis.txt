# This environment is dedicated to result analysis
mteb[beir] @ git+https://github.com/Lyon-NLP/mteb-french.git #removing the other mteb above for now (to adapt when mteb-french gets merged)
matplotlib>=3.8.2
seaborn>=0.13.1
openpyxl>=3.1.2
